services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gymlog
      POSTGRES_PASSWORD: gymlog
      POSTGRES_DB: gymlog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
